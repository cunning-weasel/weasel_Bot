{
  "description": "A Crypto Trading Bot in Node.js & MongoDb, wrapped up with Docker",
  "main": "app.js",
  "scripts": {
    "prod": "NODE_ENV=production NODE_PATH=src node src/index.js",
    "start": "NODE_PATH=src node src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^0.21.4",
    "dotenv": "^10.0.0",
    "mongoose": "^5.13.19"
  }
}
